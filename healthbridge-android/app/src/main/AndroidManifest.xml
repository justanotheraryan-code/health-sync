<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      HealthBridge is fully offline by design.
      There is intentionally NO <uses-permission android:name="android.permission.INTERNET" />.
      No backend, no accounts, no network. The app only WRITES to Google Health Connect.
    -->

    <!-- ============================================================= -->
    <!-- Health Connect permissions (12 total per PRD)                 -->
    <!-- 5x READ + 7x WRITE for the 7 supported MVP data types.        -->
    <!-- ============================================================= -->

    <!-- READ -->
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />

    <!-- WRITE -->
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_SLEEP" />
    <uses-permission android:name="android.permission.health.WRITE_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.WRITE_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_VO2_MAX" />

    <!-- Allows querying whether the Health Connect APK is installed / launchable. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthBridge">

        <!-- Single-Activity host for the Compose NavHost. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HealthBridge">

            <!-- Launcher entry point. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Health Connect privacy-policy / rationale intent-filter.
              Invoked by Health Connect when the user taps "privacy policy" in
              the permissions sheet (Android 14+ in-app rationale path).
            -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!--
          Permissions rationale activity-alias.
          On Android 13 and below, Health Connect launches the rationale via this
          alias action. Both paths resolve to MainActivity, which routes to the
          onboarding / privacy explanation screen.
        -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

    </application>

</manifest>
